import React from 'react';
import { View, Text } from 'react-native';
import { Flex } from 'antd-mobile';

const messageItem = ({ data }) => null;

const styles = {
  container: {},
};

export default messageItem;
